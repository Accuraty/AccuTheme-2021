// This needs to be the very first import in the project to make sure that the .env file
// is loaded into the global env variable before anything else tries to use it.
import dotenv from 'dotenv';
dotenv.config();
